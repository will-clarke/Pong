pub struct Score {
    pub l_score: usize,
    // r_score: usize,
}

impl Score {
    pub fn new() -> Score {
        Score {
            l_score: 0,
            // r_score: 0,
        }
    }

    // pub fn check_for_win(&self)

}
